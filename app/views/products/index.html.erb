<h1>Products#index</h1>
<% @products.each do |product| %>
  <p><%= link_to product.name, products_show_path %></p>
  <p><%= image_tag product.image.variant(resize_to_limit: [285, 285]).processed %></p>
<% end %>
<div><%= paginate @products %></div>
